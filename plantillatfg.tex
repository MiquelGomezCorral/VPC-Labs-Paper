%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                       CARGA DE LA CLASE DE DOCUMENTO                        %
%                                                                             %
% Las opciones admisibles son:                                                %
%      12pt / 11pt            (tamaño del cuerpo de letra; no usar 10pt)      %
%                                                                             %
% catalan/spanish/english     (idioma principal del trabajo)                  %
%                                                                             % 
% french/italian/german...    (si necesitáis usar otro idioma adicional)      %
%                                                                             %
% listoffigures               (El documento incluye un Índice de figuras)     %
% listoftables                (El documento incluye un Índice de tablas)      %
% listofquadres               (El documento incluye un Índice de cuadros)     %
% listofalgorithms            (El documento incluye un Índice de algoritmos)  %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[11pt,spanish,listoffigures,listoftables]{tfgetsinf}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                     CODIFICACIÓN DEL ARCHIVO FUENTE                         %
%                                                                             %
%    Windows suele usar 'ansinew'                                             %
%    en Linux es posible que sea 'latin1' o 'latin9'                          %
%    Pero lo más recomendable es usar utf8 (unicode 8)                        %
%                                          (si vuestro editor lo permite)     % 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[utf8]{inputenc} 


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                       OTROS PAQUETES Y DEFINICIONES                         %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{glossaries}
\usepackage{textcomp}
\usepackage{booktabs}
\usepackage{float}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{tabularx}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% Listings para código
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  frame=single,
  breaklines=true,
  postbreak=\mbox{\textcolor{gray}{$\hookrightarrow$}\space},
  columns=fullflexible,
  keepspaces=true,
  numbers=none
}

% Bibliografía
\usepackage[backend=biber,style=numeric,sorting=none]{biblatex}
\addbibresource{bibliografia.bib}

% Hyperref siempre al final
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=cyan,
  citecolor=black
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                          DATOS DEL TRABAJO                                  %
%                                                                             %
% título alumno, titor y curso académico                                      %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\title{VPC Laboratorios: Gender Recognition, Car Model identification \& Attention Visualization}
\author{Gómez Corral, Miquel}
% \tutor{No}
% \curs{MUIARFID}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                     PARAULES CLAU/PALABRAS CLAVE/KEY WORDS                  %
%                                                                             %
% Independentment de la llengua del treball, s'hi han d'incloure              %
% les paraules clau i el resum en els tres idiomes                            %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\keywords{
    Gender Recognition; Car Model Identification; Attention Visualization; Transformers; Convolutional Neural Networks
} % Paraules clau 
{
   Gender Recognition; Car Model Identification; Attention Visualization; Transformers; Convolutional Neural Networks
} % Palabras clave
{
   Gender Recognition; Car Model Identification; Attention Visualization; Transformers; Convolutional Neural Networks
} % Key words


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                                                             %
%                              INICI DEL DOCUMENT                             %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%              RESUMENES DEL TFG EN VALENCIA, CASTELLA I ANGLES               %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% \begin{abstract}[spanish]

% \end{abstract}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                                                             %
%                              CONTENIDO DEL TRABAJO                          %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                  ÍNDICE                                     %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% \clearpage
% \tableofcontents
% \listoffigures
% \listoftables


\mainmatter

\chapter{Introducción}
En esta memoria se presentan los trabajos seleccionados para la asignatura de Visión por Computador: Gender Recognition, Car Model Identification y Attention Visualization.

Se presentan los pasos que se han seguido para llegar a ellos y los resultados obtenidos en cada uno de los ejercicios.

\subsubsection{Hardware utilizado}
Los experimentos se han realizado en mi máquina personal: GPU NVIDIA RTX 5070 (12 GB VRAM), procesador AMD Ryzen 9 9000X, 64 GB de RAM y Ubuntu 24.04 LTS.

Todos los modelos han podido ser entrenados y evaluados sin problemas. 

\section{Código entregado}
Se han entregado 4 notebooks, cada uno correspondiente a un ejercicio.   Estos se encuentran en la carpeta \texttt{/notebooks} junto al resto del repositorio con el código necesario para ejecutar cada uno de los ejercicios.

\begin{itemize}
  \item \texttt{gender\_prediction.ipynb}: Ejercicio 1 predicción de género.
  \item \texttt{car\_model\_identification.ipynb}: Ejercicio 2 identificación de modelos de coches.
  \item \texttt{attention\_visualization.ipynb}: Ejercicio 3 visualización de atención.
  \item \texttt{attention\_visualization\_interactive.ipynb}: Ejercicio 3 pero con plots interactivos, cuando das click a un pixel, se cambia la visualización a su token.
\end{itemize}

 El código asociado a estos ejercicios se puede encontrar en \texttt{/app} repartidos en sus correspondientes subcarpetas. El punto de acceso a estos es el archivo \texttt{main.py} que se encarga de cargar los modelos y ejecutar los experimentos necesarios para cada ejercicio.

 Está auto documentado y se puede usar siguiendo las instrucciones del README para instalar las dependencias y ejecutar cada uno de los ejercicios.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                        Notebooks base                                     %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\chapter{Ejercicios}
\section{Ejercicio 1: Gender Recognition}

Para realizar esta tarea, se ha usado como referencia el paper adjunto en el enunciado \\\texttt{https://www.sciencedirect.com/science/article/abs/pii/S0167865515003979}, ha sido de gran ayuda.

Ya que la tarea se dividía en dos objetivos, se ha intentado conseguir primero el score de $\geq 98\%$ accuracy en validación, para luego reducir el tamaño del modelo y conseguir el objetivo de $95\%$ accuracy con menos de 100k parámetros.

\subsection{Datos}
Los datos provistos venían en un formato muy cómodo de numpy y ya divididos en entrenamiento y test (test = validación). Se han cargado y se han convertido en un dataset de PyTorch para facilitar su uso en el entrenamiento de los modelos y poder aplicar técnicas de data augmentation.

\subsection{Métodos probados}

En la tabla \ref{tab:accuracy_ej1} se aprecia un resumen de los resultados obtenidos con cada técnica probada. Parte por parte se desglosa en el siguiente apartado. 

Con una pipeline de entrenamiento estándar, se han probado diferentes arquitecturas de CNN, con diferentes hiperparámetros y técnicas de regularización. 

Siguiendo el paper de referencia y sus nombres, se ha iniciado con una CNN del formato '\textit{I}', con \texttt{torch.optim.SGD}, \texttt{torch.nn.CrossEntropyLoss} y sin técnicas de regularización. Se ha modificado ligeramente para quitar la capa de \texttt{AdaptiveAvgPool2d} a (8,8) que añadían en el paper. Esta causaba que el entrenamiento fuera inestable y dependía de factores aleatorios como la semilla para entrenar bien.

Esto nos dejaba con valores 'bajos' de accuracy, de un 93\%, por lo que se han ido probando las siguientes técnicas de regularización:

\begin{itemize}
  \item Usar AdamW en vez de SGD.
  \item Usar label smoothing con $\epsilon = 0.1$.
  \item Usar data augmentation con técnicas de RandomRotation, RandomHorizontalFlip, RandomGrayscale, RandomResizedCrop, RandomErasing, GaussianNoise y ColorJitter
\end{itemize}

La mayoría de estas han terminado mejorando el accuracy, aunque algunas como el ColorJitter o el RandomResizedCrop solo ralentizaban el entrenamiento, sin llegar a dar una mejora al modelo.

Con esto, se ha llegado a superar el $96.0\% - 96.5\%$ de accuracy en validación, pero aún estaba lejos del objetivo de 98\%. En el paper, se usan varias versiones de la misma CNN pero entrenadas con seeds y usadas como un ensemble. Sin embargo, se ha querido implementar una mejora más al proceso antes de llegar a esto.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{images/beforebest.png} 
  \caption{Muestras fallidas por el modelo antes de aplicar ensemble y aumento específico.}
  \label{fig:gender_samples}
\end{figure}

Si nos fijamos, aparte de no saber si Michael Jackson es hombre o mujer, el modelo también tiene problemas con personas con gafas, gorras y en general con aquellas a las que no se les ve el pelo (aunque no todos son así, claro). Esto nos lleva a pensar que depende mucho de estas características para hacer la predicción, y que si no las tiene, el modelo se confunde.

Por eso, se ha implementado un \textbf{aumento de datos específico} que, a partir del concepto del RandomErasing, pretende centrarse en zonas específicas de la imagen, en concreto en la zona del pelo. Con este aumento, se consigue pasar de un 96.5\% a un 97.1\% de accuracy en validación. 

Aún así, no se ha conseguido llegar al 98\% de accuracy, por lo que se ha decidido probar a ajustar el umbral de predicción y hacer inferencia usando cada imagen y su versión invertida horizontalmente. Con esto alcanzamos un 97.3\% de accuracy, solo usando este modelo.

Luego, siguiendo las indicaciones del paper, se ha implementado un ensemble de 3 modelos con diferentes seeds, AdamW, label smoothing y los data augmentation que han demostrado funcionar incluyendo la personalizada, subiendo un pelín el accuracy a 97.5\%. 

Al ver que se llegaba a un tope, se ha cambiado directamente la arquitectura del modelo. Visto que el paper llega a un tope con el setup actual, se ha usado una ResNet con 3 skip connections y dos convoluciones por bloque.

Con esta nueva arquitectura, se ha conseguido llegar a un 98.4\% de accuracy en validación, y con el ensemble de 3 modelos, se ha llegado a un 98.6\%, superando el objetivo marcado. Tras algunas pruebas se ha superado esta marca añadiendo otra capa de convolución a cada bloque, llegando a un 98.6\% con un solo modelo y a un 98.8\% con un ensemble, siendo este el mejor resultado obtenido.

\subsection{Modelo pequeño}
Para conseguir el objetivo de un modelo con menos de 100k parámetros y un accuracy de al menos 95\%, se ha optado por usar exactamente la misma arquitectura que la usada antes de la ResNet, pero esta vez usando un \texttt{AdaptiveAvgPool2d((6, 6))} como entrada a la parte densa del modelo y 61 canales en vez de 64 en la última capa convolucional, lo que a su vez reduce el número de neuronas de la primera capa densa.

Esto hace que el número de parámetros se reduzca a 99.0K, y con la misma pipeline de entrenamiento, aumento de datos y label smoothing, se ha conseguido fácilmente que el accuracy en validación se mantenga por encima del 95\%, llegando a un 96.8\%.

\begin{table}[H]
\centering
\begin{tabular}{llr}
	\toprule
	\textbf{Modelo} & \textbf{Técnica} & \textbf{Accuracy \%} \\
	\midrule
  Modelo I & Base sin \texttt{AdaptiveAvgPool2d} & 93.0 \\
	& Mejores técnicas & 96.5 \\
  & Aumento específico & 97.1 \\
  & Horizontal flipping & 97.3 \\
  & Todo + Ensemble & 97.6 \\
	\midrule
  ResNet 1 & Todo & 98.2 \\
  & Todo + Ensemble & 98.6 \\
  ResNet 2 & Todo & 98.6 \\
  & Todo + Ensemble & \textbf{98.8} \\
	\midrule
  Modelo I Pequeño & Todo & \textbf{96.8} \\
  & Todo + Ensemble* & 97.0 \\
	\bottomrule
\end{tabular}
\caption{Resultados de accuracy para diferentes técnicas aplicadas. Todo es la combinación de AdamW, label smoothing, data augmentation y aumento específico. Ensemble es la combinación de 3 modelos con diferentes seeds entrenados con esta pipeline. * Al ser 3 modelos, se supera el límite de 100k parámetros, pero cada modelo individualmente se mantiene por debajo de este límite.}
\label{tab:accuracy_ej1}
\end{table}

Como vemos, el tamaño del modelo no ha tenido tanta importancia a la hora de conseguir un buen accuracy y ha sido más la configuración del entrenamiento, los data augmentation y el buen uso del modelo en inferencia.


\section{Ejercicio 2: Car Model Identification}
Para esta tarea, partimos usando el código del ejercicio anterior, toda la pipeline de entrenamiento y data augmentation se han mantenido (menos el aumento de datos personalizado), aunque se han ajustado algunos hiperparámetros como el learning rate.

Ahora la diferencia será que no solo usamos una CNN, sino dos y ya entrenadas. Usaremos una ResNet-50 y una VGG-16 preentrenadas en ImageNet tanto para crear una red bilineal con ambas como para solo usar una y crear redes siamesas.

\subsection{Datos}
De forma similar al ejercicio anterior, los datos venían en formato numpy y ya divididos en entrenamiento y test (validación). Se han cargado y convertido a un dataset de PyTorch para facilitar su uso en el entrenamiento de los modelos.

Ahora, estas imágenes tienen un tamaño de 250x250. Por los tamaños de las salidas de las CNNs usadas, se han reescalado a 224x224 para que su último mapa convolucional devuelva una salida de 7x7 (ambas), que es lo que se necesita para la parte bilineal del modelo.

\subsection{Entrenamiento}
Como ya sabemos, para entrenar una red bilineal usando dos CNNs ya entrenadas, únicamente tenemos que combinar las salidas de ambas CNNs, hacer un producto externo entre todos los canales de las salidas. Este tensor se proyecta a un vector y se normaliza según el tamaño del mapa convolucional, que en este caso es de 7x7, y se le aplica una raíz cuadrada seguida de una normalización L2. 

Finalmente, este vector tendrá un tamaño equivalente a multiplicar los canales de ambas redes (por ejemplo, $2048 \times 512 = 1.048.576$ características si cruzamos la ResNet y la VGG), y se pasa a una sola capa lineal que da 20 salidas, una por cada clase.

Para hacer efectivo el entrenamiento de esta red bilineal, se congelan las dos redes convolucionales, y se entrena solo la capa lineal final hasta que se estabilice. Luego, se reduce el learning rate en dos órdenes de magnitud y se descongelan las CNNs para hacer un fine-tuning de toda la red.

\subsection{Técnicas probadas}
Con el mismo proceso de entrenamiento que el ejercicio anterior, no se ha tenido que cambiar apenas nada y se han podido probar los tres posibles enfoques para esta tarea sin mucha dificultad: Una red bilineal con ResNet-50 y VGG-16, una red siamesa con ResNet-50 y otra red siamesa con VGG-16. 

\textbf{Un detalle importante:} para hacer las redes siamesas no se cargan dos modelos idénticos en memoria (lo cual sería un desperdicio enorme de recursos). Simplemente se carga la red una vez, y para sacar las características se hace el producto externo de su salida consigo misma. Luego el backpropagation se encarga de actualizar los pesos de la red para que aprenda a sacar características que sean útiles para esta tarea.


\begin{table}[H]
\centering
\begin{tabular}{lrr}
	\toprule
	\textbf{Técnica} & \textbf{Accuracy\% Fase 1} & \textbf{Accuracy\% Fase 2} \\
	\midrule
  VGG-16 siamesa & 65.94 & 68.11 \\
  ResNet-50 siamesa & 49.36 & 67.73 \\
  Bilineal VGG-16 y ResNet-50 & 65.94 & \textbf{73.72} \\
	\bottomrule
\end{tabular}
\caption{Resultados de accuracy para diferentes redes}
\label{tab:accuracy_ej2}
\end{table}

Tras el entrenamiento de solo la última capa (Fase 1), se obtenían unos resultados de accuracy bastante buenos para la VGG y la mixta, rondando el 65.94\%, mientras que la ResNet sola arrancó más floja. Luego, tras descongelar las CNNs y hacer el fine-tuning (Fase 2), la precisión subió en todos los frentes, destacando la combinación Bilineal (VGG + ResNet) que alcanzó casi un 74\%.

Como se puede observar, usar una sola CNN consigue algo menos de accuracy que juntar dos distintas. Es lógico, ya que dos arquitecturas diferentes 'ven' cosas distintas en la imagen y se complementan mejor. Sin embargo, usar una sola red juega muy a favor de reducir el uso de memoria y potencia de cálculo, lo que lo convierte en un enfoque mucho más práctico y eficiente si vamos justos de recursos.

\clearpage

\section{Ejercicio 3: Attention Visualization}

% - Modelos: 'google/vit-base-patch16-224', 'vit_base_patch16_224'
% - La attention se extrae de forma diferente según el modelo, cada uno tiene un arquítectura distinta, por lo que se han tenido que adaptar los métodos de extracción a cada modelo. 
% - A partir de ahí sacamos con los mapas extraidos los plots son iguales
% - Attenion rollot formulas
% - Con ayuda de claude se implementa las visualizaciones: Mean por capa y max por capa general, luego, lo mismo por cabeza
% - Todo se centra en el token CLS
% - Rollout lo mismo con mean y max
% - Por último, se ha implementado una visualización interactiva, donde al hacer click en un pixel de la imagen, se muestra la atención de ese pixel a lo largo de las capas y cabezas del modelo, para ver como va cambiando el foco de atención a lo largo del modelo de cada patch en vez del token de clasifiación.

% luego por separado resultado y insights

% - Se ven 20 imágenes, 2 de cada una de las 10 clases interesantes (listarlas)
% - Clasifican todo bien (solo poner una linea)
% - Con el rollout se ve como se fijan en partes importantes de la imagen: ruedas en el bus, apertura del volcan, alas de la mariposa, en el coral algo interesante se centra en el fondo xq es como que ve si está bajo del mar, en la gitarra en el abujero o en el pavo real en los patrones de las plumas...
% - Los dos modelos muestran comportamientos casi identicos, con el modelo de google dando una 'atención más intepretable' y el otro más 'difusa', pero ambos se centran en las mismas zonas de la imagen, lo que demuestra que ambos modelos han aprendido a fijarse en las partes relevantes de la imagen para hacer su predicción. Ya que las del modelo do google son más 'limpias', se han usado estas para las visualizaciones, aunque las del otro modelo también se pueden encontrar en el notebook. Aquí comparación (guitar compartion guitar_google vs guitar_timm images on rollout)
% - Luego por capas y cabeza, se ve muy bien como primero se este foco va cambiando a lo largo de las capas. ALgunas cabezas 'parecen no atender a nada' y otras se centran en partes muy concretas de la imagen, como por ejemplo una cabeza que se centra en las ruedas del bus, otra que se centra en el pico del ave, otra que se centra en el volcan... Es curioso ver como cada cabeza tiene un comportamiento completamente distinto y se centran en cosas distintas de la imagen, lo que demuestra la capacidad de atención de estos modelos.
% - Las cabezas y capas que parecen no atender a nada, parece ser por que en esa imagen no hay nada relevante para ellas.
% - La 2nd capa parece ser la que mejor atiende a los objetos de la imagen, mientras que las capas más profundas parecen ser más 'abstractas'

% - Luego, si no fijamos en la visualización interactiva, se ve como cada patch va cambiando su atención a lo largo de las capas, y como al principio se centran en zonas muy cercanas a ellos mismos, por lo que el modelo ha aprendido 'cosas a nivel espacial' de donde están las cosas. a medida que avanzan las capas, se van fijando en zonas más amplias, lo que demuestra la capacidad de estos modelos para ir integrando información de distintas partes de la imagen a medida que avanzan las capas.

%  Curiosidades:
% - el volcan con un reflejo en el agua, el patch del reflejo se centra primero en sí mismo, según avanzan las capas, se va fijando en el volcan, lo que demuestra que el modelo ha aprendido a relacionar el reflejo con el objeto que lo produce, lo cual es bastante impresionante. (all images of volcano)
% - En la mariposa, los patches de la flor se atienden entre ellos, casi segmentandola. Lo mismo con el café de la 'expreso', la zebra del fondo que sale en una de las imágenes (3 images on vertical of the zebra, on the second column three more of the flower)


% - En el rollout por patch no parece haber mucha diferencia con el del CLS, lo que demuestra que el modelo no se centra solo en el token de clasificación, sino que cada patch también tiene su propia atención y va cambiando su foco a lo largo de las capas.


\subsection{Métodos}

Para este ejercicio se han usado dos modelos ViT (\textit{Vision Transformer}): \texttt{google/vit-base-patch16-224}, cargado desde HuggingFace mediante \texttt{transformers}, y \texttt{vit\_base\_patch16\_224}, cargado desde \texttt{timm}. Ambos son funcionalmente equivalentes: dividen la imagen de entrada en parches de $16 \times 16$ píxeles, lo que sobre una imagen de $224 \times 224$ produce una rejilla de $14 \times 14 = 196$ parches, más el token especial \texttt{[CLS]} usado para la clasificación. Esto da lugar a una secuencia de 197 tokens, y a matrices de atención de forma $(1, 12, 197, 197)$ por capa y modelo.

La forma en que se extrae la atención difiere entre modelos. Con el modelo de Google, \texttt{ViTModel} expone directamente las matrices de atención de cada capa si se pasa \texttt{output\_attentions=True}. Con el modelo de \texttt{timm}, esto no está disponible directamente, por lo que se ha registrado un \textit{forward hook} sobre las capas \texttt{attn.qkv}, reconstruyendo manualmente la atención a partir de los tensores $Q$, $K$ y $V$. A partir de ahí, una vez extraídas las atenciones, el pipeline de visualización es idéntico para ambos modelos.

La extracción se ha implementado a mano. Con ayuda de Claude, tres tipos de visualización:

\begin{itemize}
  \item \textbf{Atención por capa:} para cada capa del modelo, se agrega la atención del token \texttt{CLS} sobre todos los parches, usando la media o el máximo entre las 12 cabezas. Se proyecta el vector de 196 valores resultante a una rejilla de $14 \times 14$ y se interpola bicúbicamente a $224 \times 224$ para una visualización más suave, superponiéndolo sobre la imagen original.

  \item \textbf{Atención por cabeza:} en vez de agregar, se visualizan las 12 cabezas por separado para una capa dada, lo que permite ver qué parte de la imagen atiende cada cabeza de forma individual.

  \item \textbf{Attention rollout:} propaga la atención a lo largo de todas las capas del modelo para estimar el flujo de información acumulado. Se calcula de forma recursiva como:

  \begin{equation}
    A_{\text{rollout}}^{l} = \left(A^{l} + I\right) \cdot A_{\text{rollout}}^{l-1}, \quad l > 1
  \end{equation}
  \begin{equation}
    A_{\text{rollout}}^{1} = A^{1} + I
  \end{equation}

  donde $A^{l}$ es la matriz de atención agregada en la capa $l$ e $I$ es la identidad, añadida para modelar las conexiones residuales. La fila correspondiente al token de interés da el mapa de atención acumulado. Al igual que antes, se computa tanto con media como con máximo entre cabezas.
\end{itemize}

Por último, se ha implementado una \textbf{visualización interactiva} en el notebook \\ \texttt{attention\_visualization\_interactive.ipynb}: al hacer clic sobre un píxel de la imagen, se muestra la atención del parche correspondiente a ese píxel en vez de la del token \texttt{CLS}, permitiendo explorar cómo cada parche distribuye su atención a lo largo de las capas del modelo.

\clearpage

\subsection{Resultados y análisis}

Se han visualizado 20 imágenes, 2 por cada una de las siguientes 10 clases: \textit{zebra}, \textit{dálmata}, \textit{mariposa monarca}, \textit{guitarra acústica}, \textit{autobús escolar}, \textit{volcán}, \textit{margarita}, \textit{pavo real}, \textit{coral cerebro} y \textit{cafetera espresso}. Todos los ejemplos son clasificados correctamente por ambos modelos.

\subsubsection{Comparación entre modelos}

Ambos modelos muestran un comportamiento prácticamente idéntico en cuanto a las zonas de la imagen a las que atienden. La diferencia principal es de calidad visual: el modelo de Google produce mapas de atención más limpios e interpretables, mientras que el de \texttt{timm} tiende a ser más difuso, como se puede apreciar en la Figura \ref{fig:guitar_comparison}. Dado que ambos se fijan en las mismas regiones relevantes, se ha optado por usar el modelo de Google para todas las visualizaciones presentadas en esta sección, aunque las del modelo \texttt{timm} también están disponibles en el notebook.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.85\textwidth}
    \includegraphics[width=\textwidth]{images/guitar_google.png}
    \caption{Modelo Google}
  \end{subfigure}
  \vfill
  \begin{subfigure}[b]{0.85\textwidth}
    \includegraphics[width=\textwidth]{images/guitar_timm.png}
    \caption{Modelo timm}
  \end{subfigure}
  \caption{Attention rollout sobre la guitarra acústica. El modelo de Google produce una atención más limpia y concentrada, mientras que el de timm es más difuso, aunque ambos clasifican correctamente.}
  \label{fig:guitar_comparison}
\end{figure}

\subsubsection{Attention rollout: zonas relevantes por clase}

Con el rollout se aprecia con claridad que el modelo no atiende a la imagen de forma uniforme, sino que concentra la atención en las partes semánticamente relevantes para cada clase. En el autobús escolar se destacan las ruedas y la carrocería, en el volcán la boca, en la mariposa las alas, en la guitarra el agujero de la caja de resonancia... Un caso especialmente curioso es el del coral cerebro: la atención se centra en el fondo de la imagen, lo cual tiene sentido dado que el fondo acuático es un indicador contextual muy fuerte de que se trata de un organismo marino.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.75\textwidth}
    \includegraphics[width=\textwidth]{images/volcan_rollout.png}
    \caption{Volcán rollout}
  \end{subfigure}
  \vfill
  \begin{subfigure}[b]{0.75\textwidth}
    \includegraphics[width=\textwidth]{images/peackock_rollout.png}
    \caption{Pavo real rollout}
  \end{subfigure}
  \vfill
  \begin{subfigure}[b]{0.75\textwidth}
    \includegraphics[width=\textwidth]{images/coral1_rollout.png}
    \caption{Coral rollout}
  \end{subfigure}
  \caption{Atención del token \texttt{CLS} en la capa 2 para distintas clases. Se puede observar cómo el modelo se centra en las partes más relevantes de cada imagen para su clasificación.}
  \label{fig:rollout_examples}
\end{figure}

\subsubsection{Análisis por capas y cabezas}

Al visualizar la atención capa a capa, se observa que la capa 2 es consistentemente la que produce mapas más interpretables y focalizados sobre el objeto principal (Figura \ref{fig:layer2_examples}), mientras que las capas más profundas tienden a mostrar patrones más abstractos y distribuidos. Esto es coherente con la literatura: las capas iniciales capturan características espaciales locales, y las capas finales integran información de alto nivel.
\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/bus1_layer2.png}
    \caption{Autobús escolar Layer 2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/bus1_layer8.png}
    \caption{Autobús escolar Layer 8}
  \end{subfigure}

  \vfill

  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/daisy1_layer2.png}
    \caption{Daisy Layer 2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/daisy1_layer8.png}
    \caption{Daisy Layer 8}
  \end{subfigure}

  \vfill

  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/peackock_layer2.png}
    \caption{Pavo real Layer 2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{images/peackock_layer8.png}
    \caption{Pavo real Layer 8}
  \end{subfigure}
  \caption{Atención del token \texttt{CLS} en las capas 2 y 8 para distintas clases. La capa 2 produce mapas focalizados y nítidos; en la capa 8 la atención es más abstracta y distribuida.}
  \label{fig:layer2_examples}
\end{figure}

El análisis por cabezas resulta especialmente revelador. Como se puede ver en la Figura \ref{fig:volcano_heads}, cada cabeza especializa su atención en aspectos distintos de la imagen: en el volcán, por ejemplo, una cabeza se centra en el pico, otra en el contorno y otra en la zona de las plantas. Esta especialización es una de las propiedades más llamativas de los mecanismos de atención multi-cabeza.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/volcano_heads_layer2.png}
  \caption{Las 12 cabezas de atención en la capa 2 sobre una imagen de volcán.}
  \label{fig:volcano_heads}
\end{figure}

Algunas cabezas parecen no atender a ninguna región concreta (Figura \ref{fig:volcano_heads}). La interpretación más razonable es que esas cabezas están especializadas en características que no aparecen en la imagen en cuestión: si una cabeza ha aprendido a detectar, por ejemplo, agua o texto, en una imagen que no los contiene simplemente no tiene nada relevante a lo que atender. Esto además pasa con más frecuencia en las capas más profundas, lo que refuerza la idea de que estas capas capturan características más abstractas y de alto nivel, que pueden no estar presentes en todas las imágenes (Figura \ref{fig:attending_nothing} capa 8 vs capa 2 del volcán).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/zebra1_heads_layer8-attending-nothing.png}
  \caption{Ejemplo de cabezas que no atienden a ninguna región relevante, capa 8.}
  \label{fig:attending_nothing}
\end{figure}

\subsubsection{Visualización interactiva y atención por parche}

Usando la visualización interactiva, se puede explorar cómo cada parche distribuye su atención a lo largo de las capas. En las capas iniciales, cada parche atiende principalmente a sus vecinos más cercanos, lo que indica que el modelo aprende primero relaciones espaciales locales. A medida que avanzan las capas, la atención se va expandiendo hacia regiones más amplias, integrando progresivamente información de distintas partes de la imagen, un comportamiento típico de los transformers.

Comparando el rollout por parche con el del token \texttt{CLS}, no se aprecian diferencias drásticas en las zonas destacadas, lo que sugiere que la información de atención no está concentrada únicamente en el token de clasificación, sino que se distribuye de forma coherente entre todos los tokens.

Algunos casos concretos resultan especialmente interesantes. En las imágenes del volcán con reflejo en el agua (Figura \ref{fig:volcano_reflection}), los parches que corresponden al reflejo atienden inicialmente a sí mismos y a su entorno inmediato. Sin embargo, a medida que avanzan las capas, su atención se desplaza progresivamente hacia el volcán real, lo que demuestra que el modelo ha aprendido a relacionar el reflejo con el objeto que lo produce. Este tipo de razonamiento visual implícito es difícil de conseguir con arquitecturas más simples, y resulta bastante llamativo.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_1.png}
    \caption{Capa 1}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_3.png}
    \caption{Capa 3}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_5.png}
    \caption{Capa 5}
  \end{subfigure}

  \vspace{0.3cm}

  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_6.png}
    \caption{Capa 6}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_10.png}
    \caption{Capa 10}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/volcano_reflection_layer_11.png}
    \caption{Capa 11}
  \end{subfigure}
  \caption{Evolución de la atención del parche correspondiente al reflejo del volcán a lo largo de las capas.}
  \label{fig:volcano_reflection}
\end{figure}

En la imagen de la mariposa monarca, los parches correspondientes a la flor sobre la que descansa la mariposa atienden entre sí, llegando casi a segmentar la flor del resto de la imagen (Figura \ref{fig:butterfly_flower}). Un efecto similar ocurre con la cebra que aparece en el fondo de una de las imágenes de cebra: sus parches de fondo se reconocen entre sí, o con la zona de la taza de café en la imagen de la cafetera espresso.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/butterfly_flower_layer_1.png}
    \caption{Flor, capa 1}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/butterfly_flower_layer_5.png}
    \caption{Flor, capa 5}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/butterfly_flower_layer_9.png}
    \caption{Flor, capa 9}
  \end{subfigure}

  \vspace{0.3cm}

  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/zebra_back_layer_1.png}
    \caption{Cebra de fondo, capa 1}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/zebra_back_layer_2.png}
    \caption{Cebra de fondo, capa 2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.30\textwidth}
    \includegraphics[width=\textwidth]{images/zebra_back_layer_3.png}
    \caption{Cebra de fondo, capa 3}
  \end{subfigure}
  \caption{Evolución de la atención del parche de la flor en la imagen de la mariposa monarca y con la cebra que aparece en el fondo de otra imagen.}
  \label{fig:butterfly_flower}
\end{figure}

Estos resultados, en conjunto, ponen de manifiesto la capacidad de los modelos ViT para aprender representaciones espaciales ricas y coherentes sin supervisión explícita sobre la estructura de la imagen, simplemente a partir del mecanismo de atención y el entrenamiento supervisado por clasificación.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                                                             %
%                                BIBLIOGRAFIA                                 %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\cleardoublepage
\printbibliography

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                                                             %
%                                 APÉNDICES                                   %
%                                                                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\APPENDIX


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                       EJEMPLOS DE CADA TIPO DE FACTURA                     %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% \chapter{Apéndice correspondencia notebooks}
% \label{appendix:notebooks}
% ...

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                              FIN DEL DOCUMENTO                              %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{document}

